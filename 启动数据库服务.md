# 启动 MongoDB 和 Redis 服务指南

## 前提条件

1. **确保 Docker Desktop 正在运行**
   - 打开 Docker Desktop 应用程序
   - 等待 Docker 完全启动（系统托盘图标显示为运行状态）

## 方法 1：使用 Docker Compose（推荐，最简单）

### 步骤：

1. **打开 PowerShell 或命令提示符**，进入项目目录：
   ```powershell
   cd D:\AI-Traiding\TradingAgents-CN
   ```

2. **启动 MongoDB 和 Redis**：
   ```powershell
   docker-compose up -d mongodb redis
   ```

3. **验证服务是否运行**：
   ```powershell
   docker ps
   ```
   应该看到 `tradingagents-mongodb` 和 `tradingagents-redis` 容器在运行。

4. **查看服务状态**：
   ```powershell
   docker-compose ps
   ```

### 服务信息：

- **MongoDB**:
  - 端口: `27017`
  - 用户名: `admin`
  - 密码: `tradingagents123`
  - 数据库: `tradingagents`
  - 连接字符串: `mongodb://admin:tradingagents123@localhost:27017/tradingagents`

- **Redis**:
  - 端口: `6379`
  - 密码: `tradingagents123`
  - 连接字符串: `redis://:tradingagents123@localhost:6379`

---

## 方法 2：使用项目提供的批处理脚本

### 步骤：

1. **双击运行脚本**：
   ```
   scripts\docker\start_docker_services.bat
   ```
   或
   ```
   scripts\startup\start_debug_services.bat
   ```

2. 脚本会自动：
   - 检查 Docker 是否运行
   - 启动 MongoDB 容器
   - 启动 Redis 容器
   - 显示服务状态

---

## 方法 3：使用 Docker 命令手动启动

### 启动 MongoDB：

```powershell
docker run -d `
    --name tradingagents-mongodb `
    -p 27017:27017 `
    -e MONGO_INITDB_ROOT_USERNAME=admin `
    -e MONGO_INITDB_ROOT_PASSWORD=tradingagents123 `
    -e MONGO_INITDB_DATABASE=tradingagents `
    -v mongodb_data:/data/db `
    --restart unless-stopped `
    mongo:4.4
```

### 启动 Redis：

```powershell
docker run -d `
    --name tradingagents-redis `
    -p 6379:6379 `
    -v redis_data:/data `
    --restart unless-stopped `
    redis:latest redis-server --appendonly yes --requirepass tradingagents123
```

---

## 验证服务是否正常运行

### 检查 MongoDB：

```powershell
# 检查容器状态
docker ps | findstr mongodb

# 测试连接
docker exec -it tradingagents-mongodb mongosh -u admin -p tradingagents123
```

### 检查 Redis：

```powershell
# 检查容器状态
docker ps | findstr redis

# 测试连接
docker exec -it tradingagents-redis redis-cli -a tradingagents123 ping
```

应该返回 `PONG`。

---

## 停止服务

### 使用 Docker Compose：

```powershell
docker-compose stop mongodb redis
```

### 使用 Docker 命令：

```powershell
docker stop tradingagents-mongodb tradingagents-redis
```

### 停止并删除容器：

```powershell
docker stop tradingagents-mongodb tradingagents-redis
docker rm tradingagents-mongodb tradingagents-redis
```

---

## 常见问题

### 1. Docker Desktop 未运行

**错误信息**：
```
error during connect: Get "http://%2F%2F.%2Fpipe%2FdockerDesktopLinuxEngine/...": open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.
```

**解决方法**：
- 打开 Docker Desktop 应用程序
- 等待 Docker 完全启动（系统托盘图标显示为运行状态）

### 2. 端口已被占用

**错误信息**：
```
Error: bind: address already in use
```

**解决方法**：
- 检查是否有其他服务占用了 27017 或 6379 端口
- 或者修改 docker-compose.yml 中的端口映射

### 3. 容器名称冲突

**错误信息**：
```
Error: container name "tradingagents-mongodb" is already in use
```

**解决方法**：
```powershell
# 停止并删除现有容器
docker stop tradingagents-mongodb tradingagents-redis
docker rm tradingagents-mongodb tradingagents-redis

# 然后重新启动
```

### 4. 查看容器日志

```powershell
# MongoDB 日志
docker logs tradingagents-mongodb

# Redis 日志
docker logs tradingagents-redis
```

---

## 启动可选的管理界面

### Redis Commander（Redis 管理界面）：

```powershell
docker-compose up -d redis-commander
```

访问：http://localhost:8081

### Mongo Express（MongoDB 管理界面）：

```powershell
docker-compose up -d mongo-express
```

访问：http://localhost:8082
- 用户名: `admin`
- 密码: `tradingagents123`

---

## 数据持久化

数据会自动保存在 Docker volumes 中：
- MongoDB 数据: `mongodb_data` volume
- Redis 数据: `redis_data` volume

即使删除容器，数据也不会丢失。重新创建容器时会自动恢复数据。

---

## 快速启动命令（复制粘贴）

```powershell
# 进入项目目录
cd D:\AI-Traiding\TradingAgents-CN

# 启动服务
docker-compose up -d mongodb redis

# 等待几秒让服务启动
timeout /t 5

# 检查状态
docker ps --filter "name=tradingagents-"
```

---

## 下一步

服务启动后，可以运行应用：

```powershell
python -m app
```

应用应该能够成功连接到 MongoDB 和 Redis 了！

