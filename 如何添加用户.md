# 如何添加用户

## 方法 1：使用脚本创建默认管理员（最简单）

### 创建默认管理员用户

```powershell
# 进入项目目录
cd D:\AI-Traiding\TradingAgents-CN

# 设置编码（避免中文显示问题）
$env:PYTHONIOENCODING='utf-8'

# 创建默认管理员（admin/admin123）
python scripts\create_default_admin.py
```

### 创建自定义管理员用户

```powershell
python scripts\create_default_admin.py --username myuser --password mypass123 --email myuser@example.com
```

### 覆盖已存在的用户

```powershell
python scripts\create_default_admin.py --overwrite
```

### 列出所有用户

```powershell
python scripts\create_default_admin.py --list
```

---

## 方法 2：通过 API 创建用户（需要后端服务运行）

### 使用脚本通过 API 创建

```powershell
# 确保后端服务正在运行
python -m app

# 在另一个终端运行
python scripts\create_default_users.py
```

### 手动通过 API 创建

```powershell
# 注册新用户
curl -X POST "http://localhost:8000/api/auth/register" `
  -H "Content-Type: application/json" `
  -d '{"username":"newuser","email":"newuser@example.com","password":"password123"}'
```

---

## 方法 3：使用导入配置脚本（包含系统配置）

```powershell
# 只创建默认用户，不导入配置
python scripts\import_config_and_create_user.py --create-user-only

# 从配置文件导入并创建用户
python scripts\import_config_and_create_user.py install\database_export_config.json
```

---

## 默认管理员账户信息

创建成功后，默认管理员账户为：

- **用户名**: `admin`
- **密码**: `admin123`
- **邮箱**: `admin@tradingagents.cn`
- **角色**: 管理员
- **配额**: 10000 次/天
- **并发限制**: 10 个

⚠️ **重要**: 首次登录后请立即修改密码！

---

## 常见问题

### 1. 缺少 pymongo 模块

**错误信息**:
```
ModuleNotFoundError: No module named 'pymongo'
```

**解决方法**:
```powershell
pip install pymongo
```

### 2. MongoDB 连接失败

**错误信息**:
```
❌ 错误: MongoDB 连接失败
```

**解决方法**:
1. 确保 MongoDB 服务正在运行
2. 检查 Docker 容器状态: `docker ps | findstr mongodb`
3. 如果未运行，启动服务: `docker-compose up -d mongodb`

### 3. 用户已存在

**错误信息**:
```
⚠️  用户 'admin' 已存在
```

**解决方法**:
- 使用 `--overwrite` 参数覆盖现有用户
- 或使用不同的用户名创建新用户

### 4. 编码问题（中文显示乱码）

**解决方法**:
```powershell
$env:PYTHONIOENCODING='utf-8'
python scripts\create_default_admin.py
```

---

## 用户管理脚本参数说明

### create_default_admin.py

```powershell
python scripts\create_default_admin.py [选项]

选项:
  --username USERNAME    用户名（默认: admin）
  --password PASSWORD     密码（默认: admin123）
  --email EMAIL          邮箱（默认: <username>@tradingagents.cn）
  --overwrite            覆盖已存在的用户
  --list                 列出所有用户
```

### 示例

```powershell
# 创建默认管理员
python scripts\create_default_admin.py

# 创建自定义管理员
python scripts\create_default_admin.py --username admin2 --password pass123 --email admin2@example.com

# 查看所有用户
python scripts\create_default_admin.py --list

# 重置管理员密码
python scripts\create_default_admin.py --overwrite --password newpassword123
```

---

## 验证用户创建成功

### 方法 1：使用脚本查看

```powershell
python scripts\create_default_admin.py --list
```

### 方法 2：直接连接 MongoDB 查看

```powershell
# 进入 MongoDB 容器
docker exec -it tradingagents-mongodb mongosh -u admin -p tradingagents123

# 切换到数据库
use tradingagents

# 查看用户
db.users.find().pretty()
```

### 方法 3：通过 API 验证

```powershell
# 测试登录
curl -X POST "http://localhost:8000/api/auth/login" `
  -H "Content-Type: application/json" `
  -d '{"username":"admin","password":"admin123"}'
```

---

## 下一步

用户创建成功后：

1. **启动后端服务**（如果未运行）:
   ```powershell
   python -m app
   ```

2. **访问前端**:
   - 打开浏览器访问前端地址
   - 使用创建的账户登录

3. **修改密码**:
   - 登录后立即修改默认密码
   - 在用户设置中修改密码

4. **创建更多用户**:
   - 使用管理员账户登录后，可以通过前端界面创建新用户
   - 或使用上述脚本创建

---

## 相关文件

- `scripts/create_default_admin.py` - 创建管理员用户脚本
- `scripts/create_default_users.py` - 通过 API 创建用户脚本
- `scripts/import_config_and_create_user.py` - 导入配置并创建用户脚本
- `app/services/user_service.py` - 用户服务类
- `app/routers/auth_db.py` - 用户认证路由

