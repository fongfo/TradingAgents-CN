# TradingAgents-CN 前端启动指南

> **版本**: v1.0.0-preview  
> **技术栈**: Vue 3 + Vite + TypeScript + Element Plus

## 📋 前置要求

### 必需软件

1. **Node.js >= 18.0.0**
   ```bash
   node --version  # 应显示 v18.x.x 或更高
   ```

2. **npm >= 8.0.0**
   ```bash
   npm --version  # 应显示 8.x.x 或更高
   ```

### 后端服务

确保后端服务已启动并运行在 `http://localhost:8000`

## 🚀 快速启动

### 方式一：使用 npm（推荐）

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖（首次运行需要）
npm install

# 3. 启动开发服务器
npm run dev
```

### 方式二：使用启动脚本

```bash
# 在项目根目录运行
python scripts/startup/start_frontend.py
```

### 方式三：使用 yarn（如果已安装）

```bash
cd frontend
yarn install
yarn dev
```

## 🌐 访问前端

启动成功后，在浏览器中访问：

**前端地址**: http://localhost:3000

### 默认配置

- **前端端口**: 3000
- **后端API**: http://localhost:8000/api
- **代理配置**: 自动代理 `/api` 请求到后端

## 📝 详细步骤

### 步骤 1: 安装依赖

首次运行需要安装依赖包：

```bash
cd frontend
npm install
```

**安装时间**: 约 2-5 分钟（取决于网络速度）

**常见问题**:
- 如果安装失败，尝试使用国内镜像：
  ```bash
  npm install --registry=https://registry.npmmirror.com
  ```

### 步骤 2: 启动开发服务器

```bash
npm run dev
```

**预期输出**:
```
  VITE v5.0.10  ready in 500 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
  ➜  press h to show help
```

### 步骤 3: 访问前端界面

1. 打开浏览器
2. 访问 http://localhost:3000
3. 首次访问会跳转到登录页面

## ⚙️ 配置说明

### 环境变量

前端使用 Vite 的环境变量系统，可以在 `frontend/.env.local` 中配置：

```env
# API 基础 URL（默认：http://localhost:8000）
VITE_API_BASE_URL=http://localhost:8000/api

# 应用标题
VITE_APP_TITLE=TradingAgents-CN
```

### 代理配置

开发环境已配置自动代理，所有 `/api` 请求会自动转发到后端：

```typescript
// vite.config.ts
proxy: {
  '/api': {
    target: 'http://localhost:8000',
    changeOrigin: true
  }
}
```

## 🔧 开发命令

### 开发模式

```bash
npm run dev
```

- 启动开发服务器
- 支持热模块替换（HMR）
- 自动打开浏览器（可选）

### 构建生产版本

```bash
npm run build
```

构建产物在 `frontend/dist/` 目录

### 预览生产版本

```bash
npm run preview
```

预览构建后的生产版本

### 代码检查

```bash
npm run lint      # ESLint 检查
npm run format    # Prettier 格式化
npm run type-check # TypeScript 类型检查
```

## 🎯 功能模块

### 主要页面

- **仪表板** (`/dashboard`) - 数据概览和统计
- **股票筛选** (`/screening`) - 多维度股票筛选
- **股票分析** (`/analysis`) - 单股/批量分析
- **队列管理** (`/queue`) - 任务队列监控
- **分析报告** (`/reports`) - 报告查看和导出
- **系统设置** (`/settings`) - 配置管理
- **自选股** (`/favorites`) - 自选股管理

### 登录认证

首次访问需要登录：
- 默认用户名/密码请查看后端配置
- 或通过后端 API 创建用户

## 🐛 常见问题

### 1. 端口被占用

**错误信息**:
```
Error: listen EADDRINUSE: address already in use :::3000
```

**解决方法**:
```bash
# 方法1：修改端口（临时）
npm run dev -- --port 3001

# 方法2：关闭占用端口的进程
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:3000 | xargs kill -9
```

### 2. 依赖安装失败

**解决方法**:
```bash
# 清理缓存
npm cache clean --force

# 删除 node_modules 和 lock 文件
rm -rf node_modules package-lock.json

# 使用国内镜像重新安装
npm install --registry=https://registry.npmmirror.com
```

### 3. 后端连接失败

**错误信息**:
```
Network Error
Failed to fetch
```

**解决方法**:
1. 确认后端服务已启动：访问 http://localhost:8000/api/health
2. 检查 CORS 配置：确保后端允许前端域名
3. 检查代理配置：确认 `vite.config.ts` 中的代理设置正确

### 4. 类型错误

**解决方法**:
```bash
# 重新生成类型文件
npm run type-check

# 如果仍有问题，重新安装依赖
rm -rf node_modules
npm install
```

### 5. 构建失败

**解决方法**:
```bash
# 清理构建缓存
rm -rf dist node_modules/.vite

# 重新构建
npm run build
```

## 📱 浏览器支持

- ✅ Chrome/Edge (最新版本)
- ✅ Firefox (最新版本)
- ✅ Safari (最新版本)
- ⚠️ IE 11 (不支持)

## 🔒 安全提示

1. **开发环境**: 仅用于本地开发，不要在生产环境使用
2. **API 密钥**: 不要在前端代码中硬编码 API 密钥
3. **HTTPS**: 生产环境必须使用 HTTPS

## 📚 相关文档

- [后端启动指南](./项目启动指南.md)
- [前端开发指南](../development/v0.1.16/frontend-guide.md)
- [API 文档](http://localhost:8000/docs)

## 🆘 获取帮助

如果遇到问题：

1. **查看日志**: 检查浏览器控制台和终端输出
2. **检查网络**: 确认后端服务正常运行
3. **查看文档**: 参考 [完整文档目录](../README.md)
4. **提交 Issue**: https://github.com/hsliuping/TradingAgents-CN/issues

---

**祝您使用愉快！** 🎉

